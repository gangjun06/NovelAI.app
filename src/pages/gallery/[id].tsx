import { MainTemplate } from '~/components/template'

const GalleryImageDetail = () => {
  return (
    <MainTemplate title={''} description={''}>
      <div></div>
    </MainTemplate>
  )
}

export default GalleryImageDetail
